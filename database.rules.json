{
  "rules": {
    "users": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['name'])"
      }
    },

    "approved": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth.token.email === 'leejunhyuk0205@gmail.com'"
      }
    },

    "rejected": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth.token.email === 'leejunhyuk0205@gmail.com'"
      }
    },

    "symbols": {
      ".read": true,
      "$k": {
        ".write": "auth != null && (root.child('approved/' + auth.uid).val() === true || auth.token.email === 'leejunhyuk0205@gmail.com')",
        ".validate": "newData.isString()"
      }
    },

    "holdings": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && (root.child('approved/' + auth.uid).val() === true || auth.token.email === 'leejunhyuk0205@gmail.com')"
      }
    },

    "shared": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('friends/' + $uid + '/' + auth.uid).exists())",
        ".write": "auth != null && auth.uid === $uid && (root.child('approved/' + auth.uid).val() === true || auth.token.email === 'leejunhyuk0205@gmail.com')"
      }
    },

    "friends": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        "$friend": {
          ".write": "auth != null && (auth.uid === $uid || auth.uid === $friend) && (root.child('approved/' + auth.uid).val() === true || auth.token.email === 'leejunhyuk0205@gmail.com')",
          ".validate": "newData.isBoolean()"
        }
      }
    },

    "invites": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null && (root.child('approved/' + auth.uid).val() === true || auth.token.email === 'leejunhyuk0205@gmail.com') && ((!data.exists() && newData.child('from').val() === auth.uid) || (data.exists() && data.child('from').val() === auth.uid))",
        ".validate": "newData.hasChildren(['from', 'exp'])"
      }
    }
  }
}
