{
  "rules": {
    "config": {
      "owner": {
        ".read": "auth != null",
        ".write": "auth != null && !data.exists() && newData.val() === auth.uid"
      }
    },

    "users": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['name'])"
      }
    },

    "approved": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && root.child('config/owner').val() === auth.uid"
      }
    },

    "holdings": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid && (root.child('approved/' + auth.uid).val() === true || root.child('config/owner').val() === auth.uid)"
      }
    },

    "shared": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || root.child('friends/' + $uid + '/' + auth.uid).exists())",
        ".write": "auth != null && auth.uid === $uid && (root.child('approved/' + auth.uid).val() === true || root.child('config/owner').val() === auth.uid)"
      }
    },

    "friends": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        "$friend": {
          ".write": "auth != null && (auth.uid === $uid || auth.uid === $friend) && (root.child('approved/' + auth.uid).val() === true || root.child('config/owner').val() === auth.uid)",
          ".validate": "newData.isBoolean()"
        }
      }
    },

    "invites": {
      "$code": {
        ".read": "auth != null",
        ".write": "auth != null && (root.child('approved/' + auth.uid).val() === true || root.child('config/owner').val() === auth.uid) && ((!data.exists() && newData.child('from').val() === auth.uid) || (data.exists() && data.child('from').val() === auth.uid))",
        ".validate": "newData.hasChildren(['from', 'exp'])"
      }
    }
  }
}
